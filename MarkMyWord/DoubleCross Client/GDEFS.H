#ifndef _GDEFS
#define _GDEFS

#define HD_BACKGROUND 4021

#ifdef HOYLE_CG2

enum { DIAMONDS = 1, CLUBS, HEARTS, SPADES };

enum { TWO = 2, THREE, FOUR, FIVE, SIX, SEVEN, EIGHT, NINE, TEN, JACK, QUEEN, KING, ACE };

enum { SOUTH = 0, WEST, NORTH, EAST };

enum { PT_NONE = -1, PT_HUMAN = 0, PT_BEGINNER, PT_INTERMEDIATE, PT_ADVANCED };

# endif

class GDefs
{

public:	

enum{ HUMAN, COMPUTER };

enum{
		PLAYR1,
		PLAYR2,
		PLAYR3,
		PLAYR4,
		PLAYR5,
		PLAYR6,
		PLAYR7,
		PLAYR8,
		PLAYR9,
		PLAYR10
};

enum{ BEGINNER, INTERMEDIATE, EXPERT };

enum{ DARK, LIGHT };

enum {	NORTH,
		SOUTH,
		EAST,
		WEST,
		NORTHEAST,
		NORTHWEST,
		SOUTHEAST,
		SOUTHWEST
};

enum{ EMPTY, FULL };

enum{ UP, DOWN, RIGHT, LEFT };

enum{ VERTICAL, HORIZONTAL };

enum{ FORWARD, REVERSE };

enum{ HUMANVSCOMP, HUMANVSHUMAN, COMPVSCOMP, MULTIPLAYER };

enum{ SERIOUS, LIGHTCHAT, TALKATIVE };

enum{ OFF, ON };

enum{ SPADE, CLUB, DIAMOND, HEART };
enum{ LOW, HIGH };
enum{ PASSINGLEFT, PASSINGRIGHT, PASSINGACROSS, NOPASSING };

enum{
	MEL,
	ELAYNE,
	LANGLEY,
	MIRANDA,
	MARTY,
	ROBIN,
	MAURICE,
	SPACECOUPLE,
	PRIMUS,
	WAYNE,
	BART
};

enum{ SAVE, SAVEAS, RESTORE, REVERT };

enum{ WINTER, SPRING, SUMMER, FALL };

enum{ DEAL_SPEED, THINK_SPEED, TRICK_SPEED, CARD_SPEED };

enum{ PLAYED, PASSED };

};

#endif //_GDEFS
